<!DOCTYPE html>
<html>
<head>
	<title>RailsBlog</title>
	<%= stylesheet_link_tag    "rails_blog/application", media: "all" %>
	<%= javascript_include_tag "rails_blog/application" %>
	<%= csrf_meta_tags %>
	<link href='http://fonts.googleapis.com/css?family=Lobster' rel='stylesheet' type='text/css'>
	<link href='http://fonts.googleapis.com/css?family=Open+Sans:300italic,700italic,800italic,400,300,800,700' rel='stylesheet' type='text/css'>
	<link href='http://fonts.googleapis.com/css?family=Cousine' rel='stylesheet' type='text/css'>
</head>
<body>
	<%= render 'layouts/rails_blog/header' %>
	<div class="container-fluid">
		<div class="row">
			<div class="sidebar col-sm-3 hidden-xs">
				<% if @sidebar_partial && @sidebar_partial != RailsBlog::UnderTheHoodPostsController::SIDEBAR_NAMESPACE %>
					<%= render @sidebar_partial %>
				<% end %>
				<%= content_for :sidebar %>
			</div>
			<div class="col-sm-9 col-sm-offset-3 col-xs-12 content-box">
				<!-- For nested layouts use content_for :content -->
				<%= content_for?(:content) ? yield(:content) : yield %>
				<%= yield(:comment_section) if content_for?(:comment_section) %>
			</div>
		</div>
	</div>
</body>
</html>
